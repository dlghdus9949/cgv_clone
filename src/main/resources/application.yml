spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cgv_db?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: dmsxor7@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect  # MySQL에 맞는 Dialect 설정
        show_sql: true
        format_sql: true
        jdbc:
          time_zone: Asia/Seoul

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

#    database-platform:
#    Upload file Setting